{
  "name": "outweigh",
  "version": "0.1.0",
  "private": true,
  "description": "Outweigh — an endless higher-or-lower game about company size.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "build:values": "tsx scripts/build-values.ts",
    "check:contrast": "node scripts/check-contrast.mjs",
    "check:size": "node scripts/check-size.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.1",
    "vitest": "^5.0.1",
    "yahoo-finance2": "^2.12.0"
  }
}
