# Build report — 2026-09-24 (bootstrap)

- Tickers attempted: 320
- Valid: 320
- Dropped: 0

## Note

This `public/values.json` is a **local synthetic bootstrap** (deterministic
placeholder values) so the site builds and tests run before the first real
data fetch. It is NOT real market data.

Run the **Build values** workflow once (Actions › Build values › Run workflow)
to replace it with real Yahoo Finance market caps. The daily action then
refreshes it every day at 02:00 UTC. Never ship placeholder numbers as real.
